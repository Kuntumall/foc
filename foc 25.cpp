#include<stdio.h>
int main()
{
	int num1,num2;
	printf("enter the num1 :");
	scanf("%d",&num1);
		
			scanf("%d",&num2);
			if (num1>num2)
			printf("the num1 is greater :");
			else
			printf("the num2 is greater ");
		return 0;
}
